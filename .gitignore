node_modules/
.env
*.log
npm-debug.log*
.DS_Store
.claude/

# Llave privada de firma de la extension (NO subir nunca) y .crx generado
extension-deploy/*.pem
extension-deploy/*.crx
installer/nssm.exe

# Config local de extensiones (IP y clave secreta) — usar config.example.js como plantilla
extension/**/config.js

# Capturas de pantalla locales
docs/Capturas/
